# This is a sample Python script.
from Design.BrowserHistory import BrowserHistory

if __name__ == '__main__':
    pass

    # call = ["BrowserHistory", "visit", "visit", "visit", "back", "back", "forward", "visit", "forward", "back", "back"]
    # sub = [["leetcode.com"], ["google.com"], ["facebook.com"], ["youtube.com"], [1], [1], [1], ["linkedin.com"], [2],
    #        [2], [7]]
    # ans = []
    # obj = BrowserHistory(sub[0][0])
    # for i in range(1, len(call)):
    #     fun = call[i]
    #     val = sub[i][0]
    #     if fun == "visit":
    #         obj.visit(val)
    #     elif fun == "back":
    #         ans.append(obj.back(val))
    #     else:
    #         ans.append(obj.forward(val))
    # print(ans)
